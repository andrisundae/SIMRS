{
  "name": "simrs-client",
  "version": "1.0.0",
  "description": "Aplikasi SIMRS Client",
  "main": "index.js",
  "author": "LINKAR Team Development",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "wsrun --exclude-missing start",
    "desktop": "wsrun -p @simrs/main -c desktop",
    "compile": "wsrun compile",
    "start-desktop": "concurrently \"yarn start\" \"yarn desktop\"",
    "build-desktop": "wsrun -p @simrs/main -c build-desktop",
    "prebuild-desktop": "concurrently \"wsrun -p @simrs/billing -c build\" \"wsrun -p @simrs/system -c build\"",
    "storybook": "wsrun -p @simrs/components -c storybook"
  },
  "workspaces": {
    "packages": [
      "app/*",
      "module/*",
      "shared/components",
      "shared/common"
    ],
    "nohoist": [
      "**/dotenv",
      "**/dotenv/**",
      "**/electron",
      "**/electron/**",
      "**/electron-is-dev",
      "**/electron-is-dev/**",
      "**/electron-devtools-installer",
      "**/electron-devtools-installer/**",
      "**/babel-loader",
      "**/babel-loader/**"
    ]
  },
  "devDependencies": {
    "concurrently": "^4.1.1",
    "wsrun": "^5.0.0"
  }
}
